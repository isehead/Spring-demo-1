<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd">

    <!--Load the properties file-->
    <context:property-placeholder location="classpath:sports.properties"/>

    <!-- Define your beans here -->

    <!--
      Add a logger config to see logging messages.
      - For more detailed logs, set values to "FINEST"
      - For info on logging levels, see: http://www.vogella.com/tutorials/Logging/article.html
   -->

    <!-- Defining the dependency -->

    <bean id="myLoggerConfig" class="com.springdemo.MyLoggerConfig" init-method="initLogger">
        <property name="rootLoggerLevel" value="FINE"/>
        <property name="printedLoggerLevel" value="FINE"/>
    </bean>

    <!-- this works in HelloSpring -->

    <bean id="myFortuneService"
          class="com.springdemo.CheckFortuneService">
    </bean>

    <bean id="myCoach"
          class="com.springdemo.TrackCoach">
        <constructor-arg ref="myFortuneService"/>
    </bean>

    <!-- my own implementation -->

    <bean id="mySampleFortuneService"
          class="com.springdemo.FortuneServiceMySample">
    </bean>

    <bean id="fbCoach"
          class="com.springdemo.FootballCoach">
        <constructor-arg ref="mySampleFortuneService"/>
    </bean>

    <bean id="myCricketCoach"
          class="com.springdemo.CricketCoach">
        <property name="fortuneService" ref="myFortuneService"/>

        <!-- Injecting literals -->
        <property name="emailAddress" value="${sports.email}"/>
        <property name="team" value="${sports.team}"/>
    </bean>

</beans>